import { ServiceCards } from "./ServiceCards"

export const ServiceComponent = () => {
    return (
        <section className="services">
            <ServiceCards
            />
        </section>
    )
}
